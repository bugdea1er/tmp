---
name: tests
"on":
  push:
    branches: [main, 113-move-behaviour]
  pull_request:

jobs:
  test:
    runs-on: ${{ matrix.os }}
    strategy:
      fail-fast: false
      matrix:
        os:
          - ubuntu-20.04
          - ubuntu-24.04
          - macos-12
          - macos-14
          - windows-2019
          - windows-2022
    steps:
      - name: Set up `tmpfs` (if supported)
        run: |
          sudo mkdir /com.github.bugdea1er.tmp
          sudo mount tmpfs -t tmpfs -o size=100k /com.github.bugdea1er.tmp
          echo $TMPDIR
          df -h
        if: ${{ matrix.os == 'ubuntu-20.04' }}
      - name: Checkout repository
        uses: actions/checkout@v4
      - name: Configure CMake
        run: cmake -B build -DCMAKE_COMPILE_WARNING_AS_ERROR=ON
      - name: Build tests
        run: cmake --build build
      - name: Run tests
        run: ctest --output-on-failure --test-dir build
